pykotor>=1.8.0
numpy>=1.22
PyOpenGL~=3.1
PyGLM>=2.0,<2.8; python_implementation == 'CPython'

# Optional dependencies (install separately or via optional-dependencies):
# moderngl>=5.8.0  # Modern OpenGL wrapper with C extensions for improved rendering performance